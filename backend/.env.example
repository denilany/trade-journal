# Server
PORT=8080
ENV=development

# CORS
FRONTEND_ORIGIN=http://localhost:3000

# JWT
JWT_ACCESS_SECRET=change_me_access
JWT_ISSUER=yourapp
JWT_AUDIENCE=yourapp-web
JWT_ACCESS_TTL_MIN=15

# Refresh tokens
REFRESH_TOKEN_TTL_DAYS=30
COOKIE_DOMAIN=localhost
COOKIE_SECURE=false
COOKIE_SAMESITE=lax

# Database
DATABASE_URL=postgres://user:password@localhost:5432/yourapp?sslmode=disable